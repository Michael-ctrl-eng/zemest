# Core
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic[email]==2.10.4
pydantic-settings==2.7.1

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.1
psycopg2-binary==2.9.11

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.3
python-multipart==0.0.20

# HTTP Client
httpx==0.28.1

# Task Queue
celery[redis]==5.4.0
redis==5.2.1

# Web Crawling & Content Extraction
trafilatura==2.0.0
beautifulsoup4==4.12.3

# Templating
jinja2==3.1.5

# PageIndex (self-hosted) + LLM routing
litellm>=1.82.0
pyyaml>=6.0

# Voice transcription (local, free)
faster-whisper>=1.0.0

# Admin panel + security middleware
sqladmin>=0.19.0
slowapi>=0.1.9
geoip2>=4.8.0

# Utilities
python-dotenv==1.0.1
aiofiles==24.1.0
structlog>=24.1.0

# Testing
pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0
aiosqlite==0.20.0
# Real-world simulation tests
hypothesis>=6.100.0
schemathesis>=3.36.0
locust>=2.31.0
pytest-playwright>=0.5.0
mutmut>=3.0.0

# Email
aiosmtplib==3.0.2

# Fuzzy matching
python-Levenshtein==0.26.1

# Headless browser for JS-rendered sites
playwright==1.58.0

# Multi-dialect language engine (optional — engine falls back to regex
# detection if these are not installed, but installing them enables
# 26-class Arabic dialect ID and GlotLID sentence-level LID).
camel-tools>=1.5.0
fasttext-wheel>=0.2.5

# Python 3.9 compatibility
eval_type_backport==0.3.1
